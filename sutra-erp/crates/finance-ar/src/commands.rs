//! Accounts Receivable stubs.

// commands.rs — command structs for AR operations
pub mod commands {}

// errors.rs — AR-specific error types
pub mod errors {}

// events.rs — AR domain event handlers
pub mod events {}

// queries.rs — AR read-side query types
pub mod queries {}

// repository.rs — AR repository traits
pub mod repository {}
